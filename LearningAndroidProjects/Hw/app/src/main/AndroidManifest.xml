<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.app.school" >

    <application
        android:allowBackup="true"
        android:icon="@mipmap/school_app_icon"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="com.app.school.LoginActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name="com.app.school.SignupActivity" android:theme="@style/AppTheme.Dark" />
        <activity android:name="com.app.school.MainActivity" android:theme="@style/AppTheme.Dark" />
    </application>

</manifest>
